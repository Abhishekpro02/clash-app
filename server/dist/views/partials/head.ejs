<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #1a1a1a;
      color: #f0f0f0;
      margin: 0;
      padding: 0;
    }

    .container {
      width: 100%;
      padding: 20px;
      background: linear-gradient(135deg, #ff0000, #ffeb3b, #e91e63);
      max-width: 600px;
      margin: 40px auto;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      animation: fadeIn 2s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    .header {
      text-align: center;
      padding: 20px;
      background-color: rgba(0, 0, 0, 0.8);
      color: #ffffff;
      border-radius: 10px 10px 0 0;
      animation: slideIn 1s ease-in-out;
    }

    @keyframes slideIn {
      from {
        transform: translateY(-50px);
        opacity: 0;
      }

      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    .content {
      padding: 20px;
      background-color: rgba(255, 255, 255, 0.1);
      border-radius: 0 0 10px 10px;
      animation: fadeIn 2s ease-in-out;
    }

    .footer {
      text-align: center;
      padding: 10px 0;
      color: #888888;
      font-size: 12px;
      margin-top: 20px;
    }

    a {
      color: #ffeb3b;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>